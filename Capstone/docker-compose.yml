version: '3.9'
services:
  api:
    build:
      context: .
      dockerfile: infra/docker/api.Dockerfile
    ports:
      - '8000:8000'
  dashboard:
    build:
      context: .
      dockerfile: infra/docker/dashboard.Dockerfile
    ports:
      - '8501:8501'

