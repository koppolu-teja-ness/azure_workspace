{
  "azure_resource_type": "Microsoft.KeyVault/vaults/secrets",
  "property_mappings": [
    {
      "azure_property": "value",
      "aws_property": "SecretString",
      "required": true,
      "notes": [
        "Secret values should be rotated and encrypted with customer-managed keys when required."
      ]
    },
    {
      "azure_property": "contentType",
      "aws_property": "Description",
      "required": false,
      "notes": [
        "Use metadata tags when content type semantics are important."
      ]
    }
  ]
}
